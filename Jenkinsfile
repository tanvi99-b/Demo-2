pipeline {
    agent any

    stages {
        stage('Execute shell script') {
            steps {
                script {
                    sh 'bash script.sh'
                }
            }
        }
    }
}
